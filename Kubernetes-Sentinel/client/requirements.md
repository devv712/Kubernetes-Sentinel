## Packages
recharts | For visualizing Prometheus metrics (CPU, Memory, Requests)
framer-motion | For animating cluster nodes, pod transitions, and chaos effects

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
